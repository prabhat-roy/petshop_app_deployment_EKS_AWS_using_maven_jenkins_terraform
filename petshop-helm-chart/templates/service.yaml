apiVersion: v1
kind: Service
metadata:
  name: {{ include "petshop-helm-chart.fullname" . }}
  labels:
    {{- include "petshop-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "petshop-helm-chart.selectorLabels" . | nindent 4 }}
